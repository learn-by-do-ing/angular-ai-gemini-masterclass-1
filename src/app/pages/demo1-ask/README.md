# Demo 1: Ask

## Description
A fundamental example of integrating Google GenAI with Angular.

## Details
This demo shows the simplest way to interact with the Gemini API. It uses a standalone Angular component to capture a text prompt, send it to the model using `generateContent`, and display the raw text response.

## Keypoint
Direct SDK implementation in the component without additional abstractions.
